#include <stdio.h>

/**
  * main - Prints a sentence to the standard output
  * Description: Function prints out the string value set in the string
  * sentence. This function is not interactive with the user.
  * @void: Does not need any inputs
  * Return: 0 if successful
  */
int main(void)
{
	char *sentence = "Programming is like building a multilingual puzzle";

	puts(sentence);
	return (0);
}
