#include <stdio.h>
/**
  * main - prints all possible combinations of two two-digit numbers
  * @void: no input
  * Description: Prints all possible combinations of two two-digits
  * without repeating the digits (i.e 00 01 and 01 00 are considered
  * the same combination of two two-digits). The digits printed
  * are not the same and are printed in ascending order.
  * Return: 0 on success
  */
int main(void)
{
	int i = 0;

	while (i < 1000)
	{
		if ((i % 100) != (i / 100)
	}
	putchar('\n');

	return (0);
}
